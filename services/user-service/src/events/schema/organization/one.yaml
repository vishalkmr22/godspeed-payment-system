# ONE
http.get./schema/organization/{id}:
  summary: Fetch Organization
  description: Fetch Organization from database
  fn: com.biz.schema.organization.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

